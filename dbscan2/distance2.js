'use strict";

Cotton.DBSCAN2.distanceExpandedHistoryItem = function (oExpHistoryItem1, oExpHistoryItem2) {
  // Compute the distance between two expanded historyItems.
  return 0;
};

Cotton.DBSCAN2.distanceSession = function (oSession1, oSession2) {
  // Compute distance between two sessions
  return 0;
};

Cotton.DBSCAN2.distanceStory = function (oStory1, oStory2) {
  // Compute distance between two Stories
  return 0;
};
