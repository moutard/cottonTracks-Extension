'use strict";

Cotton.DBSCAN2.distance2 = function (oExpHistoryItem1, oExpHistoryItem2) {
  // Compute the distance between two expanded historyItems.

};

Cotton.DBSCAN2.distance2b = function (oHistoryItem1, oHistoryItem2) {
  // var oExpHistoryItem1 = Cotton.Model.HistoryItem.expand(oHistoryItem1);
  // Using the id of historyItem store int he db, we can get all the expanded
  // parameters link with this historyItem.
  // var oExpHistoryItem2 = Cotton.Model.historyItem.expand(oHistoryItem2);
  // return Cotton.DBSCAN.distance2(oExpHistoryItem1, oExpHistoryItem2);
};
