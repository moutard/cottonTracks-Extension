function Cluster(){
  this.loCenter = nil; //historyItem
  this.llClusterSet = []; //list of historyItem
  
}

Cluster.prototype.computeAverage = function(){
  // initialize each cluster with a random page
};


Cluster.prototype.assignment = function(plHistoryItems){
  // Assignment step: Assign each observation to the cluster with the closest mean
};