"use strict";

Cotton.UI.Stand.Story.Card.Map = Cotton.UI.Stand.Story.Card.Card.extend({

  init : function(sMapUrl, oHistoryItem, oGlobalDispatcher) {
    this._super(oHistoryItem, oGlobalDispatcher);
  }
});